// @flow

export type DimensionsType = {
  headerHeight: number,
  modalHeaderHeight: number,
  categoryListItem: {
    iconSize: number,
    margin: number
  }
}

export default {
  headerHeight: 60,
  modalHeaderHeight: 40,
  categoryListItem: {
    iconSize: 40,
    margin: 10
  }
}
