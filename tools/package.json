{
  "version": "0.0.1",
  "name": "tools",
  "private": true,
  "type": "module",
  "scripts": {
    "manage-metadata": "yarn run -T ts-node manage-metadata.ts",
    "git-version": "yarn run -T ts-node git-version.ts",
    "github-release": "yarn run -T ts-node github-release.ts",
    "move-release-notes": "yarn run -T ts-node move-release-notes.ts",
    "next-version": "yarn run -T ts-node next-version.ts",
    "trigger-pipeline": "yarn run -T ts-node trigger-pipeline.ts",
    "license-checker": "cd $PROJECT_CWD && yarn run -T ts-node tools/license-checker.js"
  },
  "devDependencies": {
    "@octokit/auth-app": "^6.0.0",
    "@octokit/rest": "^20.0.1",
    "@types/js-yaml": "^4.0.5",
    "@types/node": "^18.0.3",
    "@types/node-fetch": "^2.6.6",
    "commander": "^11.0.0",
    "js-yaml": "^4.1.0",
    "node-fetch": "^3.3.2"
  }
}
