{
  "name": "build-configs",
  "version": "0.0.1",
  "main": "index.ts",
  "description": "Build configs for the integreat project",
  "private": true,
  "dependencies": {
    "translations": "workspace:translations"
  },
  "devDependencies": {
    "@types/flat": "^5.0.0",
    "commander": "^8.3.0",
    "decamelize": "^5.0.1",
    "flat": "^5.0.0"
  },
  "scripts": {
    "manage": "yarn g:tsnode tools/manage.ts",
    "ts:check": "yarn g:tsc",
    "lint": "yarn g:lint",
    "lint:changed": "yarn g:lint:changed",
    "lint:fix": "yarn g:lint:fix",
    "lint:ci": "yarn g:lint:ci"
  }
}
