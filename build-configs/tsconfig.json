{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "composite": true,
    "outDir": "dist",
    "types": [
      "jest",
      "node" // for scripts in tools/
    ]
  },
  "include": ["**/*", "index.ts"],
  "references": [
    {
      "path": "../translations"
    }
  ]
}
