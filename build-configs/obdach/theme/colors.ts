import { ColorsType, commonLightColors } from '../../common/theme/colors'

const themeColor = '#C2D440'
const themeColorLight = 'rgba(194,212,64, 0.5)'

export const lightColors: ColorsType = {
  themeColor,
  themeColorLight,
  ...commonLightColors,
}
